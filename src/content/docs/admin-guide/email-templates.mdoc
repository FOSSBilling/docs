---
title: Email Templates
description: Create beautiful, branded emails with custom templates.
---

FOSSBilling uses [Twig](https://twig.symfony.com/) for email templates, so you can control both the wording and the presentation of system messages.

## Managing Templates

### Access Email Settings

1. Go to **Configuration → Settings**
2. Click **Email**

You'll see every installed email template, along with its subject, owning module, and enabled status.

### Generate Missing Templates

If templates are missing, click **Generate templates** at the bottom of the page to recreate them.

## Editing Templates

1. Click the **Edit** button on any template
2. Modify the subject and content
3. Save your changes

![Editing an email template](/img/guides/emails/template-edit.png)

### Available Variables

Templates can use any variables passed by the module. Common ones include:

- `staff.name` — Staff member name
- `client.first_name` — Client first name
- `guest.system_company.name` — Your company name
- `guest.system_company.signature` — Your email signature

{% aside type="tip" %}
Send a test email to see which variables are available for that template. The **Variables** tab shows values from the last sent email.
{% /aside %}

See [Twig Filters & Functions](/extensions-and-development/twig-filters/) for all available filters.

## Template Examples

### Markdown Format

Default templates use Markdown for simple formatting. Wrap your content with the `apply markdown_to_html` filter:

```twig
\{% apply markdown_to_html %\}
Hi \{\{ staff.name \}\},

If you are reading this email, FOSSBilling is **configured properly** and is **able to send emails**.

\{\{ guest.system_company.signature \}\}
\{% endapply %\}
```

Remove the backslashes when using this in your actual template.

### HTML Format

For full control, use HTML:

```html
<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <style>
    body {
      font-family: Arial, sans-serif;
      font-size: 14px;
      color: #333333;
    }
    h1 {
      font-size: 24px;
      margin: 0 0 20px;
    }
    .signature {
      font-style: italic;
      color: #999999;
      margin-top: 20px;
      border-top: 1px solid #cccccc;
      padding-top: 10px;
    }
  </style>
</head>
<body>
  <h1>Test email from {{ guest.system_company.name }}</h1>
  <p>Hi {{ staff.name }},</p>
  <p>If you are reading this email, FOSSBilling is <strong>configured properly</strong>.</p>
  <p class="signature">{{ guest.system_company.signature }}</p>
</body>
</html>
```

### Before and After

**Default (Markdown):**
![Default template](/img/guides/emails/default-template-preview.png)

**Custom HTML:**
![Custom template](/img/guides/emails/custom-template-preview.png)

## Storage Location

Templates exist in two places:

1. **Module source** — `modules/{Module}/templates/email/` (was `html_email/` in 0.7.x)
2. **Database** — `email_template` table (copied there when module is installed)

After installation, edit templates through the admin panel or directly in the database. Changes made only to the module files will not apply until you regenerate the templates.

## Limitations

- Variables only appear in the **Variables** tab after an email has been sent at least once
- Email client HTML support varies — test with popular clients
- Inline CSS works more reliably than `<style>` blocks

## Example Module

See the [example module](https://github.com/FOSSBilling/example-module/tree/main/templates/email) for more template examples.
